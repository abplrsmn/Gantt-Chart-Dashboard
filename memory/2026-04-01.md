- ClickUp dashboard live repo confirmed at `/home/ahgadmin/clickup-dashboard` with PM2 app `clickup-dashboard` serving `https://cc.aryaduta.com/dashboard`.
- Added Google Calendar OAuth and calendar routes to live repo: `/api/google/oauth/start`, `/api/google/oauth/callback`, `/api/google/calendar/create-event`, `/api/google/calendar/list-events`, `/api/google/calendar/delete-event`.
- Google Calendar integration verified working with live token/refresh token; event creation and deletion tested successfully.
- Added meeting/task ops routes and helpers: `/api/meetings/schedule`, `/api/ops/plan`, `/api/ops/task`, plus meeting normalization helper.
- Added ClickUp task write path in live repo via `src/lib/clickup-write.ts` and `/api/clickup/tasks/create`; direct task creation to IDEA -> Tech verified working.
- Natural-language task execution can now create tasks in ClickUp, but assignee extraction/parsing is still unreliable and group-chat auto-execution is not yet production-safe.
- Cross-context inconsistency was identified as a major issue: private session knew live backend state while group sessions answered from stale zip/snapshot assumptions.
- Created shared operational source-of-truth file `OPS_STATE.md` and updated startup guidance to read it for live systems/integration claims.
- Telegram config changed from `groupPolicy: allowlist` to `groupPolicy: open` to speed onboarding/debugging for new groups including IDEA Project; gateway restart attempt via `openclaw gateway restart` exited with code 1 and needs follow-up if config does not take effect automatically.
- Current project priority: reliability in IDEA Tech, IDEA Data & Digital, and IDEA Project groups for natural request -> parse -> execute into Google Calendar and ClickUp task creation.
