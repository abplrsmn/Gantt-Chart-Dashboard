## 2026-04-07
- User wants important ongoing work to be captured as report-worthy notes for internship reporting, not just left in raw session logs.
- Current persona updates requested and accepted by user: assistant should use a soft feminine typing style when appropriate, can use light casual slang/swears naturally in relaxed chat, and should go by the name `Cici` with internal handle `cici_bot`.
- ClickUp Dashboard / CAPEX context re-anchored: the relevant project area is the CAPEX Monitor page in the ClickUp dashboard.
- User shared a CAPEX monitoring CSV used by colleagues; key recommendation: CAPEX should be a dedicated dashboard page (`/dashboard/capex`) rather than merged into the main overview page.
- Recommended CAPEX page structure: header, summary cards, normalized CAPEX project table, gantt/timeline section, and optional detail drawer.
- Recommended CAPEX table/data fields: unit, project/description, start date, end date, progress %, execution status, schedule health, status note/latest note, ClickUp task link/ID, and last updated.
- Important data-model guidance for CAPEX: normalize raw spreadsheet status into separate fields (`progressPercent`, `executionStatus`, `scheduleHealth`, `statusNote`) instead of storing one mixed status string.
- Current ClickUp mapping based on name/start/end is acceptable as bootstrap, but long-term robust mapping should store ClickUp Task ID as the primary linkage after a match is found.
- Repo inspection from `clickup-dashboard-source.zip` shows a Next.js dashboard app; current dashboard overview lives at `src/app/dashboard/page.tsx`, and there is no existing CAPEX/Gantt page yet.
- Proposed implementation direction for the dashboard repo: add a dedicated route like `src/app/dashboard/capex/page.tsx` with supporting CAPEX components/lib files rather than overloading the existing dashboard overview page.
