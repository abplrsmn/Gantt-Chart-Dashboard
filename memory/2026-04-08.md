## 2026-04-08
- Continued CAPEX dashboard work in the ClickUp Dashboard repo.
- Reviewed the CAPEX Gantt Excel/CSV and the ClickUp board screenshot to compare the spreadsheet tasks with the ClickUp task list.
- Found the ClickUp CAPEX board is still not fully aligned with the spreadsheet: only a subset of the spreadsheet rows appear visible in the board screenshot, and some rows are renamed/truncated or missing unit prefixes.
- Confirmed the CAPEX dashboard still needs normalization so the UI shows human-friendly unit names and standardized fields instead of raw spreadsheet codes/status text.
- The CAPEX data model remains partially seeded, but the new implementation path is to hydrate CAPEX projects from ClickUp task data rather than hardcoding rows.
- Discovered the live repo is `/home/ahgadmin/clickup-dashboard` and patched the live CAPEX pipeline there rather than only the workspace mirror.
- Added live CAPEX mapping support: `/api/capex/mapping`, `/api/capex/sync`, and `/public/capex/mapping.json` as the external mapping source.
- Updated the live `CapexGanttMonitor` to fetch both `/api/capex/projects` and `/api/capex/mapping`, show mapping-unresolved counts, and use fallback data only when live sync fails.
- Verified live repo build succeeded after the CAPEX/live sync changes.
