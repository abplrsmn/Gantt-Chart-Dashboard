# 2026-04-15
- Implemented parser/router/resolver hardening for `/api/ops/task` flow.
- Added structured command parser support (`TASK | ...`, `PROJECT | ...`) and explicit selector precedence.
- Extracted shared ClickUp target resolver into `src/lib/clickup-target.ts` and wired `src/lib/clickup-write.ts` to use it.
- Hardened inbound payload intake in `src/app/api/ops/task/route.ts`:
  - accepts text from `text` or `message` (plus common alternates)
  - accepts group metadata from `groupName`/`chatTitle`/`group` and context variants
  - accepts explicit selectors from top-level payload or `target`/`context`
- Added payload test matrix doc at `docs/ops-task-payload-matrix.md` for Telegram/WA manual verification.
- Validation: `npm run build` succeeded after changes.
- Important gap remains: Telegram/WA inbound webhook service itself is likely outside this repo; forwarding verification must be done in that service.
