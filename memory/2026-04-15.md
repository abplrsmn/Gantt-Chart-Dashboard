# 2026-04-15
- User wants task/project commands in group chat to be parsed cleanly without repeated confirmation.
- Preferred behavior: if a task/project is understandable from natural language or pipe-format, ask only one short clarification when a critical field is truly ambiguous.
- Important normalization rule: strip misleading prefixes like `TASK |` or `PROJECT |` from titles, and use explicit `team` / `folder` / `list` / `assignee` / `pic` / `due` / `deadline` fields when present.
- User explicitly said this rule should be stored in both `memory/YYYY-MM-DD.md` and `MEMORY.md`, but **not** as an auto-run instruction.
- The user does not want auto-run behavior; they want cleaner parsing and less back-and-forth, while still preserving human approval.
