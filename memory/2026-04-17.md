- ClickUp dashboard live repo path confirmed as `/home/ahgadmin/clickup-dashboard`; workspace copy exists at `/home/ahgadmin/.openclaw/workspace/clickup-dashboard-work/clickup-dashboard` but is not the canonical live git clone.
- Repo remote `https://github.com/Presudeos/clickup-dashboard` was pulled into live repo; newer upstream commit reached `4f2fca5` and local patches were preserved via stash/pop.
- Important operational rule learned: for debugging live ClickUp/dashboard behavior, treat PM2/live runtime env as source of truth before `.env` / `.env.local`; local env files may drift and mislead auth diagnosis.
- Synced `.env.local` ClickUp token to match live app env token to reduce future env drift during shell tests.
- Added/updated ClickUp dashboard backend capabilities in live repo:
  - `/api/capex/sync`
  - `/api/ops/unified`
  - `/api/auth/logout`
  - `/api/clickup/tasks/update` (rename task)
  - `/api/clickup/tasks/find`
  - `/api/clickup/tasks/update-fields`
- `/api/ops/task` now delegates to unified router logic for broader task/project parsing.
- Added backend improvements for tasking:
  - CAPEX vs IDEA routing
  - CAPEX matching via summary/source-key-aware logic
  - audit log at `logs/ops-router-audit.jsonl`
  - more consistent `telegramMessage` responses
  - better natural-language task parsing, assignee extraction, date parsing, and semi-structured key-value task block parsing.
- Login/logout debugging outcome: issue after logout was caused by redirect flow; fixed by making `/api/auth/logout` clear cookie and return JSON, while dashboard logout now POSTs there then does hard client redirect to `/`.
- Verified behaviors:
  - Semi-structured task payload for IDEA Tech can create ClickUp tasks successfully.
  - Rename task endpoint works after build/restart when runtime picks up latest routes.
  - New routes appeared in Next build output, so 404s observed earlier were due to stale runtime, not missing route files.
- User feedback to remember:
  - In work groups, avoid yapping, process dumps, and speculative diagnoses.
  - Prefer short, accurate, result-first updates with minimal bubbles.
  - Do not ask for execution path/details when existing backend already supports the task and data is sufficient.
  - Reduce visible errors; verify first, then speak.
