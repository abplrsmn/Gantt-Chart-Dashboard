# 2026-04-20

- Abe minta mulai sekarang setiap aktivitas/pekerjaan harian penting dicatat ke memory file supaya detail kerja per hari tetap bisa diingat lintas session.
- Aturan kerja baru: gunakan `memory/YYYY-MM-DD.md` untuk log harian yang cukup detail; gunakan `MEMORY.md` untuk konteks jangka panjang, preferensi, keputusan penting, dan aturan kerja yang berulang.
- Context hari ini:
  - Cek repo workspace vs repo live untuk ClickUp Dashboard.
  - Ditemukan app live PM2 berjalan dari `/home/ahgadmin/clickup-dashboard`, bukan dari `/home/ahgadmin/.openclaw/workspace`.
  - Repo workspace tertinggal 11 commit dari `origin/master` dan tidak clean.
  - Repo live sudah latest (`5fee35c` == `origin/master`) tetapi working tree live tidak clean (`src/lib/capex-summary-sync.ts`, untracked `logs/ops-router-audit.jsonl`, ada stash lama).
  - PM2 app `clickup-dashboard` online, uptime ~2 hari, dijalankan via `npm start`.
  - Dari logs live terlihat error `Target list not found: CAPEX Gantt 2026` dan beberapa `Failed to find Server Action`, indikasi issue runtime/deploy mismatch, bukan sekadar repo tertinggal.
- Abe juga tanya soal memory: dijelaskan bahwa detail spesifik lintas session perlu ditulis ke file agar bisa diingat konsisten.
- Abe setuju dibuatkan file khusus untuk keep track progress / daily journaling agar ada record tertulis progres kerja.
- Dibuat file `/home/ahgadmin/.openclaw/workspace/PROGRESS.md` sebagai high-level rolling progress journal lintas hari.
