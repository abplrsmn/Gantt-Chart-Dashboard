- Worked extensively on `clickup-dashboard` CAPEX Gantt / Phase Monitoring Matrix.
- Live repo path used: `/home/ahgadmin/clickup-dashboard`; workspace repo had local changes and was behind, but live deploys were done from the live repo.
- Updated CAPEX Gantt UI multiple times: removed Hotel and All Phase filters, simplified bar timeline styling, changed bar coloring/themes repeatedly based on Abe feedback.
- Fixed one mapping issue for `LANDSCAPE : LOBBY DROP OFF` by updating `public/capex/mapping.json` row 6 to ClickUp task `86ex8cwrj` in the live repo.
- Restructured Phase Monitoring Matrix by phase-specific parameters:
  - Operational Brief (PR): Brief, Received Date, Budget/CAPEX
  - Design (HoD): Start Design Date, Design Approval (+1 month), Duration delay(-)/+, Brief, Working Drawing (+3 weeks)
  - Project Control: Tender Start, APS = SPK Released (+3 weeks), Duration delay(-)/+, Contract Amount
  - Project Management Team: Commence Date, End Contract, Actual Completion, deviation : delay(-) / +, Current Site Progress
  - Handover: BAST-1, BAST-2
- Removed Blocked tab from Phase Monitoring Matrix UI because Abe considered it invalid/noisy for this summary.
- Discovered root issue with old CAPEX source: Excel/summary parsing from notes/descriptions is unreliable and causes mixed fields; JSON-per-project/per-phase is a better source of truth.
- Confirmed ClickUp can use phase parent tasks (and potentially native milestones, though native milestone flag was not conclusively set/tested).
- Implemented new experimental/server routes in live repo for CAPEX phase workflows:
  - `/api/capex/phase-sync`
  - `/api/capex/phase-sync-sph`
  - `/api/capex/sph-pilot`
  - `/api/capex/cleanup-sph-duplicates`
  - `/api/capex/cleanup-nonphase`
  - `/api/capex/cleanup-all-but-phase`
  - `/api/capex/reset-sph-phase`
  - `/api/capex/reset-sph-hard`
- ClickUp phase parent tasks created in `CAPEX Gantt 2026` for:
  - Operational Brief
  - Design
  - Project Control
  - Project Management Team
  - Handover
- Pilot JSON approach used for SPH projects (`SPH TENNIS COURT INDOOR`, `SPH TENNIS COURT 3`) and synced to ClickUp phase parents as subtasks, but duplicate/recreate issues remained because sync logic was still append-like in some runs.
- Cleaned many old non-phase tasks and duplicate SPH tasks from `CAPEX Gantt 2026`; however, verification showed duplicates could still recur when rerunning sync unless strict replace semantics are enforced.
- Began moving dashboard to prefer SPH pilot JSON via `/api/capex/sph-pilot` for more accurate phase values instead of old parser fallbacks.
- Updated `src/lib/capex.ts` to filter out obvious phase parent/noise tasks from `/api/capex/projects`, reducing dashboard source noise; last verification showed `/api/capex/projects` total dropped to 12.
- Teams pages auto-refresh was turned off earlier in the day by removing interval-based polling from:
  - `src/app/dashboard/team/page.tsx`
  - `src/app/dashboard/team/[teamName]/page.tsx`
- Important lesson for future: for Abe’s CAPEX flow, prefer strict `JSON -> ClickUp phase structure -> dashboard` rather than `Excel/raw description parsing`, and use replace/update semantics instead of append sync to avoid duplicates.
