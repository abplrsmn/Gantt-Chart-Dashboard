# 2026-04-28

- Telegram group IDs confirmed by Abe for ClickUp/dashboard summary and reminder jobs:
  - `-1003543183529` = Tech
  - `-1003883437610` = Project Team
  - `-1003746956793` = Data & Digital
- Instruction from Abe: save these and do not forget.
- Worked on live repo `/home/ahgadmin/clickup-dashboard` as source of truth; repo was pulled forward through several commits adding DB-based project summary, formatter, sender stub, and send endpoint.
- Added DB env config for live repo and confirmed app uses PostgreSQL-based summary path with `src/lib/db.ts`, `src/lib/project-summary.ts`, `src/lib/project-summary-format.ts`, and `/api/projects/summary` plus `/api/projects/summary/send`.
- Confirmed DB `cici` schema is already populated for CAPEX/project tracking. Key tables include `projects`, `project_phases`, `project_tasks`, `master_units`, `master_phases`, `master_statuses`, etc. `project_tasks` is empty; useful CAPEX/PM data lives mostly in `project_phases`.
- Determined ClickUp remains primary operational source, but summary/reporting path now reads from DB and later uses ClickUp description fallback for missing PM fields.
- Live PM2 app `clickup-dashboard` was rebuilt/restarted multiple times. Installed missing `pg` dependency in live env so DB summary route could build and run.
- Verified Telegram sends to the three target groups succeeded via message tool with explicit `target` per group after earlier multi-target attempt fell back to the current chat.
- Compared Excel/Gantt and DB repeatedly. Key conclusion: projects/PM rows existed in DB, but several Project Management fields were blank even though the data existed in ClickUp task descriptions.
- Root cause found: many PM values (Commence Date, End Contract, Actual Completion, Current Site Progress) are stored in ClickUp task descriptions rather than native task dates/fields, so DB mirror had missed them.
- Implemented live summary fallback parsing from ClickUp task descriptions for PM fields when DB values are missing, improving summary completeness immediately.
- Repaired DB `project_phases` Project Management fields directly from ClickUp descriptions for multiple projects, including examples such as SPH TENNIS COURT 3, ALV AEI MUR 6th, LANDSCAPE items, PATHWAY REPAIR, AME wood parquet, PPR Pipe Replacement, etc.
- Patched DB handover fields (`BAST-1`) from ClickUp descriptions so handover classification became more accurate.
- Corrected DB data for `APL — 2025 : PPR Pipe Replacement` end date to `2026-03-31` per Abe’s instruction.
- Final dynamic summary logic now:
  - source = DB (with PM fallback enrichment from ClickUp description when needed)
  - Handover if `bast_1_date` or `bast_2_date` exists
  - In Progress if not handover and PM signals exist
  - Near Deadline as subset of in-progress with `days_remaining` 0..7
  - Overdue hidden/disabled for now
  - Exclude projects whose `current_site_progress` contains `aborted`
- Final summary counts after patching matched Abe’s expectation more closely: `In Progress = 11`, `Handover = 4`, `Near Deadline = 0`, `Overdue = 0`.
- Handover count reached 4 after syncing BAST-1 for `LANDSCAPE : BOULEVARD` and `ACC TENNIS COURT BENCHES`.
- Summary formatter was revised to Abe’s preferred style:
  - plain title/overview (no bot footer)
  - bullet `•`
  - one blank line between projects
  - `Start Date` / `End Date` labels
  - no `Days Remaining` when no start date
  - no negative display; use absolute phrasing like `142 days left`
- Latest final summary was sent to all three Telegram groups in the revised format.
