# 2026-05-08

- Abe asked to check GitHub for new pushes on `Presudeos/clickup-dashboard`; repo was behind 3 commits on `master`.
- Deployed latest live app code in `/home/ahgadmin/clickup-dashboard` by pulling to commit `5cec614`.
- Build initially failed in Next.js because `/dashboard/projects/list` used `useSearchParams()` without a Suspense boundary.
- Applied a minimal hotfix in `src/app/dashboard/projects/list/page.tsx` by wrapping the client content in `Suspense`, then rebuilt successfully.
- Restarted PM2 app `clickup-dashboard`; app returned online and public site responded `200 OK` at `https://cc.aryaduta.com/`.
- Important: live repo now includes a local fix that is not yet pushed upstream unless committed separately.
- Later, Abe reported another new GitHub commit. Verified remote advanced by 1 commit to `eafab45` (`feat: implement dashboard layout and project gantt visualization with role-based access control`).
- Confirmed the earlier local hotfix did not conflict with the new remote commit, then pulled `eafab45` into the live repo, rebuilt successfully, restarted PM2, and verified `https://cc.aryaduta.com/` still returned `200 OK`.
- Did a deeper scan of the live dashboard for project input + attachment behavior. Confirmed original live reality before changes:
  - dashboard UI had an Attachments section on Project Details, but it was hardcoded to show `No attachments`
  - DB already had a `project_attachments` table and attachment-related columns
  - current `/api/projects/[id]` response did not include attachments
  - current unified ops / Telegram CAPEX flow parsed text fields only and did not write inbound files/attachments into project attachments
- Implemented attachment plumbing in live repo:
  - added `src/lib/project-attachments.ts` to normalize inbound attachment payloads, resolve a project by `unit + project`, persist rows into `project_attachments`, and load attachment rows for project detail views
  - updated `src/lib/unified-ops-router.ts` so CAPEX/project ops flow now attempts to persist inbound attachment metadata when present
  - updated `/api/projects/[id]` to return attachments
  - updated `src/app/dashboard/projects/[id]/page.tsx` to render attachment cards instead of static `No attachments`
  - added auth guard helper and protected project APIs: `/api/projects/gantt`, `/api/projects/[id]`, `/api/projects/[id]/scurve`, `/api/projects/summary`, `/api/projects/summary/send`
- Verification after deploy:
  - build succeeded
  - PM2 app restarted and stayed online
  - anonymous access to protected project APIs now returns `Unauthorized`
  - inserted a temporary verification attachment row into project `249`, confirmed `/api/projects/249` returned it in the new `attachments` array, then deleted the test row
- Important caution: the new attachment flow is verified at DB/API/UI level, but not yet end-to-end from a real Telegram/project-group message containing files after deployment. Future group handling should treat attachment auto-linking as implemented but still pending one live message verification.
