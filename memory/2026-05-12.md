- In Project Team Telegram group, Abe asked to attach an inbound image to AME / AEI - NEW GYM. Copied the image to live dashboard public uploads at `/uploads/project-attachments/209/telegram-141-ame-new-gym.jpg`, inserted it into `project_attachments` for project id `209` / Project Management phase row `1029`, then patched live `/api/projects/[id]` and Project Details UI so attachments are returned/rendered instead of always showing `No attachments`. Build succeeded and PM2 `clickup-dashboard` restarted online; verified the image URL returns HTTP 200 and DB row exists.
- Abe confirmed the desired CAPEX/project scheduling mindset: create/set the overall project date range first, then divide that range into phase date segments (Operational Brief, Design, Project Control, Project Management, Handover) inside the project range. This should guide future parsing/UI/data-model work.
- Implemented CAPEX Gantt segmented bar mindset in live dashboard: `/api/capex/projects` now includes project master start/end plus phaseWindows from `project_phases`; `CapexGanttMonitor` renders a full project-range bar with colored phase segments inside it. Build succeeded and PM2 `clickup-dashboard` restarted online; public API returns 200 with phaseWindowRows.
- Pulled live ClickUp dashboard from GitHub `origin/master` to commit `9aded5e`, stashed/re-applied local live patches for project attachments and CAPEX segmented Gantt bar, rebuilt successfully, restarted PM2 `clickup-dashboard`, and verified local/public `/api/capex/projects` return 200 with 14 rows and phaseWindowRows=14.
- Refined CAPEX Gantt segmented bar behavior per Abe: project master date range renders as an empty/grey full bar first; phase segments render only when explicit ordered start/end dates exist. Operational Brief/PR segment end can use `normalized_deadline_date`, so PR 12 May-20 May 2026 can sit inside a 12 May-12 Jun 2026 project range without auto-filling the rest. Build succeeded, PM2 restarted, local/public CAPEX API returned 200.
- Abe clarified the segmented bar work should be visible specifically on live project `Tuning Openclaw` (AJK / PRJ-1054). Verified live DB has project range 12 May-12 Jun 2026 and Operational Brief/PR segment 12 May-20 May 2026. Patched `ProjectGanttDB` to render an empty grey project-range bar first, with explicit phase segment bars over it; rebuilt, restarted PM2, and verified public `/api/projects/gantt` returns Tuning Openclaw with the expected dates.
- Adjusted `ProjectGanttDB` visual sizing: empty grey project date-range bar now uses same top/height as phase bars (`top: 14px`, `height: 24px`) so Tuning Openclaw project range aligns with PR segment. Build succeeded, PM2 restarted, public `/api/projects/gantt` returned 200.
- Adjusted `ProjectGanttDB` phase bar shape to match the project date-range bar by changing phase bars from `rounded` to `rounded-full`; build succeeded, PM2 restarted, public `/api/projects/gantt` returned 200.
- After Abe confirmed option 2, deleted all live `projects` except `AJK - Tuning Openclaw` (`id=249`, `PRJ-1054`). DB transaction deleted 51 project rows; cascade left 1 project, 5 phase rows, 0 attachments/tasks/change logs, and 1 project_people row. Verified public `/api/projects/gantt` returns one row: AJK - Tuning Openclaw. `/api/capex/projects` returns 0 because it only exposes execution/PM CAPEX rows and Tuning Openclaw is currently Operational Brief.
- Removed inline text labels from `ProjectGanttDB` phase bars (including initials/Done); bars now render visually only while tooltip still retains phase details. Build succeeded, PM2 restarted, public `/api/projects/gantt` returned 200.
- Updated `ProjectGanttDB` current-date marker: removed the visible “Current Date” label and thickened the dashed red line from 1.5px to 3px. Build succeeded, PM2 restarted, public `/api/projects/gantt` returned 200.
- Made `ProjectGanttDB` project range bars and phase bars clickable/keyboard accessible; clicking a bar routes to `/dashboard/projects/[id]` (project details). Tooltip text now hints “Click bar for details”. Build succeeded, PM2 restarted, public `/api/projects/gantt` returned 200.
- Removed the top Summary/Blocker/Next Action card block from Project Details page because summary already lives in phase notes/cards. Build succeeded, PM2 restarted; project detail route redirects unauthenticated as expected and `/api/projects/249` returns 200.
- Removed the separate “Team & Stakeholders” section from Project Details because PIC/stakeholder info is already shown in Project Description fields. Build succeeded, PM2 restarted, `/api/projects/249` returned 200.
