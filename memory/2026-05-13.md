- Abe asked to commit and push current ClickUp dashboard progress to GitHub. Local workspace was behind `origin/master`; stashed local work, reset to latest `origin/master` (`9aded5e`), resolved conflicts by keeping upstream/latest versions for overlapping dashboard files, fixed duplicate `dynamic` export in `/api/capex/projects`, ran `npm ci`, verified `npm run build` succeeded, committed progress as `78ff67d chore: commit dashboard progress`, and pushed to `origin/master`. Repo now matches remote at `78ff67d`; only `.openclaw/` and `tmp/` remain untracked locally. Original stash `openclaw-progress-before-push-2026-05-13` is still kept as a safety backup.
- Abe noticed most recent progress was still missing from GitHub. Checked the live repo at `/home/ahgadmin/clickup-dashboard` and found unpushed latest work: project detail attachment API/UI, `ProjectGanttDB`/CAPEX Gantt refinements, `project-gantt`/`project-summary` updates, and uploaded AME New Gym attachment image. Rebasing/fast-forwarded live repo onto latest remote, kept live changes, verified `npm run build` succeeded, committed as `9b0f9e9 feat: push latest project gantt progress`, pushed to GitHub, then fast-forwarded workspace repo to the same commit. Remote and both local repos matched `9b0f9e9` after verification.
- Abe clarified in Project Team Telegram group that for `Tuning Openclaw`, the PR phase/task is assigned by Pak Putra.
- Updated live dashboard DB for `Tuning Openclaw` PR/Operational Brief: primary assignment changed to Pak Putra in `project_people` for Operational Brief phase row `1221`.
- Abe clarified Project Description should not show a project-level PIC. PIC is phase-specific and can differ across Operational Brief, Design, Project Control, Project Management, and Handover. Updated Project Details UI to remove Project Description PIC, add phase-specific PIC inside each phase card, and keep Assigned By separate from PIC.
- Abe clarified the final phase ownership display rule: Assigned By inside each phase card should be the PIC for that specific phase, not a separate project-level value. Updated UI mapping accordingly and added best-effort DB audit logging for Project Team/Telegram CAPEX updates into `project_change_logs` when the project resolves.
- Abe defined phase abbreviations for Project Team usage: PR = Operational Brief, HoD = Design, PC = Project Control, PM = Project Management. Updated parser/memory so users can submit phase updates using short forms.
- Fixed Project Details API people mapping so `project_people.phase_id` (project phase row id) resolves to master phase id for UI matching. This makes Operational Brief Assigned By display Pak Putra for Tuning Openclaw instead of being hidden by row-id vs phase-id mismatch. Build succeeded and PM2 restarted.
- Abe sent screenshot showing Tuning Openclaw Operational Brief card still displayed Assigned By `--`. Root cause: API returned `project_people.phase_id` as phase row id/string, while UI matched against master phase id number. Fixed API to expose `pph.phase_id::int AS phase_id` plus `phase_row_id`, and made Project Details UI compare `Number(p.phase_id)`. Build succeeded, PM2 restarted, and `/api/projects/249` verifies Pak Putra with `phase_id=1`.
- Abe reported Project Gantt hover was misleading: Tuning Openclaw master project range is 12 May-12 Jun 2026, but hover showed 12 May-24 Jun and duplicate hover information. Root cause was the selected date-range hover tooltip/native title competing with bar tooltip. Updated `ProjectGanttDB` so project range bars use one custom tooltip sourced from `projects.start_date/end_date`, removed native title duplicate, and removed selected date-range hover tooltip.
- While deploying the Project Gantt hover fix, found a live-only important patch for phase-specific PIC display: `project_people.phase_id` stores a `project_phases` row id, so `/api/projects/[id]` must join `project_phases` and expose the master `phase_id` for UI matching. Added this to repo and made the UI compare numeric phase ids.
- Abe reported S-curve date labels/range must match the master project range exactly. For Tuning Openclaw that means 12 May-12 Jun 2026; the S-curve must not snap to week boundaries or extend beyond the project master start/end. Updated `SCurveCharts` fallback point generation to start at project start and end at project end exactly, and saved this as a durable chart accuracy rule.
- Abe requested delaying Tuning Openclaw PR/Operational Brief deadline to 12 May-22 May 2026. Updated live DB `project_phases` row `1221`: `received_date=2026-05-12`, `normalized_deadline_date=2026-05-22`; added `project_change_logs` audit entry. API verifies Project Details and Gantt show UTC-shifted values corresponding to 12 May and 22 May local dates.
- Audit Log page crash diagnosis: Project Team update created a `project_change_logs.action_type = 'update'`, but the audit UI only had configs for `field_updated`/approval/deadline statuses. `ActionBadge` assumed every action_type had a config, so `update` caused the client page error "This page couldn't load". Patched Audit Log UI to support `update`/`create` aliases and safely fallback for unknown action types.
- Abe asked to turn off/remove Audit Log filters. Removed the filter tabs and action query filtering from the project Audit Log page so it always shows the full chronological history with pagination.
- Abe changed Tuning Openclaw PR/Operational Brief PIC from Pak Putra back to Pak Mike. Updated live DB `project_people` primary PIC for phase row `1221`, added audit log, and verified `/api/projects/249` returns Pak Mike for `phase_id=1`.
- Abe requested proceeding Tuning Openclaw from PR/Operational Brief to HoD phase from today through 30 May 2026. Interpreted HoD as Design phase. Updated live DB: PR/Operational Brief status completed/progress 100, Design status in_progress/progress 0, `start_design_date=2026-05-13`, `design_approval_date=2026-05-30`, project `current_phase_id=2`, `overall_progress_pct=10`; added audit log. APIs `/api/projects/249` and `/api/projects/gantt` verify current phase Design and date range (UTC-shifted display values correspond to local dates).
- Gantt phase lifecycle rule from Abe: when progressing a project from Operational Brief to Design/HoD/etc., the old phase segment must remain visible in its previous section. New/current phase segments must not visually replace or cover earlier phase bars. Gantt should show a linear phase journey across Operational Brief → Design → Project Control → Project Management → Handover.
- Gantt unified bar rule from Abe: Operational Brief and Design/next phase should look connected as one project lifecycle bar, not separate pill bars. Hover should be for the whole project bar and show the master project date range plus each phase's date/deadline, not only the individual segment.
- Fixed Gantt lifecycle tooltip overflow: tooltip should use viewport/fixed positioning and clamp within screen margins, so it does not get pushed too far upward or clipped when hovering bars near the top/bottom.
