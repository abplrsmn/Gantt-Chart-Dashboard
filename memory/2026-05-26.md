# 2026-05-26

- Abe asked how to implement "number 1" from the Hermes-style capability plan: stronger browser automation for Projectia/OpenClaw. Audited the local browser layer using the `browser-automation` skill.
- Findings: OpenClaw browser tool is enabled; default profile is `openclaw` using CDP on `http://127.0.0.1:18800`; Chromium is installed at `/usr/bin/chromium`; Node/npm are available; `npx playwright --version` works (`1.60.0` via npx).
- Started the OpenClaw-managed browser successfully in headless mode. It uses persistent profile directory `/home/ahgadmin/.openclaw/browser/openclaw/user-data`.
- Verified non-invasive live dashboard access: opened `https://cc.aryaduta.com/dashboard`, redirected to `https://cc.aryaduta.com/`, and reached the Aryaduta Dashboard login screen with fields for work email/password and a Sign In button.
- Verified automation primitives with a smoke test page: snapshot refs worked, typing into an input worked, clicking a button worked, and page state changed from `waiting` to `done`.
- Current browser-layer status: local OpenClaw/Chromium browser automation is functional for navigation, screenshots, snapshots, click/type actions, and dashboard login-screen inspection. Next blocker for authenticated dashboard/ClickUp/Google work is credential/login session handling, not missing browser infrastructure.
- Abe then chose Hermes-style capability item 3 first: memory index / recall engine. Built `scripts/memory-recall.js`, a dependency-free JSON inverted-index search over workspace memory and docs.
- Added npm shortcuts: `npm run recall:build`, `npm run recall -- "query" [limit]`, and `npm run recall:stats`. The generated `.memory-index/` directory is gitignored.
- First recall build indexed 63 files into 235 chunks with 3,913 terms across `MEMORY.md`, `memory/*.md`, `TOOLS.md`, `OPS_STATE.md`, `AGENTS.md`, `docs/`, `knowledge/`, and `skills/`.
- Verified recall search with `project attachments telegram` and `browser automation chromium dashboard`; results correctly surfaced project attachment memory/docs and the browser automation audit notes.
- Abe said the memory index is more important than the other Hermes-like upgrades, so upgraded recall to v2. Added automatic stale-index rebuild, line references, chunk ids, `--type` and `--path` filters, exact phrase boost, `--json` output, `context` mode, and a `show` command for full chunk inspection.
- Added npm shortcut `npm run recall:context`. Verified v2 with normal search, compact context output, and JSON filtered search for CAPEX/Gantt daily memory.
- Abe asked about the skill evolution pipeline and described it as "Mahoraga type shit", then asked to build it. Created `scripts/skill-evolution.js`, `docs/skill-evolution-pipeline.md`, and active helper skill `skills/skill-evolution/SKILL.md`.
- Skill evolution model: memory stores facts/context; skill proposals convert recurring workflows/corrections into supervised reusable procedures. Proposals live under `skill-proposals/` and are not active until approved into `skills/`.
- Added npm shortcuts `npm run skill:evolve` and `npm run skill:evolve:list`.
- Created first supervised proposal `skill-proposals/20260526T030505Z-capex-project-input`, using recall context from CAPEX/project workflow memory. Refined it with concrete CAPEX rules: resolve by unit + normalized project, canonical five phases, default dates-without-phase to Operational Brief, vendor input pending PIC approval, cautious attachment confirmation, and audit log expectation.
- Abe asked whether 9Router was installed. Verified `/usr/bin/9router` exists as a symlink to `/usr/lib/node_modules/9router/cli.js`; global package is `9router@0.4.59`; `9router --version` returns `0.4.59`.
- 9Router audit result: installed but not running, default port `20128` not listening, no PM2/systemd user service found, data directory `/home/ahgadmin/.9router` exists and is 12M. Local README says dashboard is `http://localhost:20128/dashboard` and OpenAI-compatible endpoint is `http://localhost:20128/v1` when running.
- Later the same morning, after Abe asked for Windows access/tunneling, rechecked and found 9Router running as `node /usr/bin/9router --no-browser --skip-update` with a `next-server` child. It was listening on `0.0.0.0:20128`; `/dashboard` returned HTTP 307 to `/login` from local, LAN IP `10.21.38.102`, and Tailscale IP `100.80.142.33`. Recommended Windows access through LAN/Tailscale first rather than a public tunnel, because 9Router is an AI provider/model gateway.
- At 11:50 WIB, Abe asked to recheck 9Router status. Current finding: 9Router is no longer listening on port `20128`; local and Tailscale curls to `:20128/dashboard` fail to connect. A `next-server` process is running on port `3000`, but its cwd is `/home/ahgadmin/clickup-dashboard`, so that is the ClickUp dashboard service, not 9Router.
- Abe approved restarting 9Router. Started it under PM2 as process `9router` using `/usr/bin/9router --no-browser --skip-update`, then ran `pm2 save`. Verified it is online, listening on `0.0.0.0:20128`, and both local `http://127.0.0.1:20128/dashboard` and Tailscale `http://100.80.142.33:20128/dashboard` return HTTP 307 to `/login`.
- Abe created a 9Router API key and asked to test usage. Authenticated chat completion works against 9Router with model `cx/gpt-5.4`; test prompt returned `pong`. The `Openclaw` combo was reachable but failed because it routed to `codex/gpt-5-codex-mini`, which this Codex/ChatGPT account rejects. Direct `cx/gpt-5.3-codex` timed out after 60s during this quick test. Recommended fixing the combo to route first to a known-good model such as `cx/gpt-5.4`.
- Follow-up 9Router model tests: direct `cx/gpt-5.5` works and returned `pong`; direct `cx/gpt-5.5-mini` fails with unsupported-model error for this Codex/ChatGPT account. The `Openclaw` combo timed out after 60s when tested again. Current likely issue: combo/fallback includes mini, and mini is not usable on this account; use `cx/gpt-5.5` only as primary for now, remove/disable mini fallback until a supported cheap fallback is identified.
- Abe clarified he meant removing the mini model from the 9Router `Openclaw` combo. Backed up `/home/ahgadmin/.9router/db/data.sqlite` to `/home/ahgadmin/.9router/db/data.sqlite.backup-20260526T131332`. The stored combo initially contained `["cx/gpt-5-codex","cx/gpt-5-codex-mini"]`; removing mini exposed that `cx/gpt-5-codex` is also unsupported for this account. Updated `Openclaw` combo to `["cx/gpt-5.5"]`, restarted PM2 process `9router`, and verified `Openclaw` returns `pong` with model `gpt-5.5`.
- Abe asked to create the recommended 9Router combo split. Backed up DB to `/home/ahgadmin/.9router/db/data.sqlite.backup-20260526T133724-combos`. Created/updated combos: `Openclaw = ["cx/gpt-5.5"]`, `Openclaw-Lite = ["cx/gpt-5.2","cx/gpt-5.5"]`, and `Openclaw-Code = ["cx/gpt-5.3-codex-low","cx/gpt-5.5"]`. Restarted 9Router and verified all three combo names appear in `/v1/models`. Full chat-completion retest hit ChatGPT/Codex account usage limit on fallback `gpt-5.5` (429, reset about 30m), so routing exists but full verification should be repeated after reset.
